{
  "name": "lucidlink-mcp",
  "version": "2.0.0",
  "description": "LucidLink Admin & Connect MCP servers for Claude Desktop",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc -p test/tsconfig.json && node --test dist-test/*.test.js",
    "test:build": "tsc && tsc -p test/tsconfig.json",
    "lucid-api": "node dist/lucid-api-server.js",
    "connect": "node dist/connect-server.js",
    "browser": "node dist/browser-server.js",
    "search": "node dist/search-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
